create database miga;

create table user (int id, Varchar(50) first_Name, Varchar(50) last_Name, Varchar(50) password, int number_vote);

create table reclamation (int id, Varchar(50) titre, Text description, int nombre_vote);

